#!/usr/bin/env python3
"""Run ledger straight from source, without installing it."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from ledger.cli import main

sys.exit(main())
