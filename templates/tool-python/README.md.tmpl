# {{NAME}}

A CLI tool.

## Setup

```bash
pip install -e .
```

Or via the staff CLI:

```bash
staff install {{NAME}}
```

## Usage

```bash
{{NAME}} "some input"
{{NAME}} --verbose "some input"
```
