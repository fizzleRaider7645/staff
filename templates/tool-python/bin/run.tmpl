#!/usr/bin/env python3
"""Wrapper to run {{NAME}} without pip install."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from {{NAME_UNDER}}.cli import main

main()
